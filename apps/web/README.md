# `apps/web/` — Next.js 챗 UI

세션별:
- S01 에서 최소 챗 UI 작성 (질문 입력 + 답변 + sources 목록)
- 이후 세션에서는 API 응답에 추가된 필드만 UI 에 반영

예정 파일:
- `Dockerfile` (Node 20-alpine, `--platform linux/amd64`)
- `package.json`, `next.config.js`, `tsconfig.json`
- `app/page.tsx`, `app/api/chat/route.ts` (또는 직접 백엔드 호출)

> placeholder — 실제 코드는 후속 구현 단계.
