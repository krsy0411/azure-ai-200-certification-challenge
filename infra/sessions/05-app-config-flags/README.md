# S05 — App Configuration Bicep

세션 문서: [docs/sessions/05-app-config-flags.md](../../../docs/sessions/05-app-config-flags.md)

예정 파일: `main.bicep`, `main.bicepparam`.

배포 자원: App Configuration (Standard) · key/value · KV reference (S01 KV 재사용) · Feature flag (`enable_semantic_cache`, `enable_pg_backend`) · UAMI 에 `App Configuration Data Reader`.

> placeholder — 실제 Bicep 은 후속 구현 단계에서 작성.
